const gameTitle = "GaMemory";

export default function GameTitle(): JSX.Element {
    return (
        <h1 className="font-poppins text-4xl md:text-6xl text-emerald-600 font-extrabold">{gameTitle}</h1>
    );
}
